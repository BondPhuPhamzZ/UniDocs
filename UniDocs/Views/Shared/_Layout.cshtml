<!doctype html>
<html lang="vi">
@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
}
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>@ViewData["Title"] - UniDocs</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <link rel="stylesheet" href="~/css/style.css" />
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        .navbar-nav .nav-link.active {
            font-weight: 700 !important;
            border-bottom: 2px solid var(--bs-primary);
            color: var(--bs-primary) !important;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">

    <!-- ================= NAVBAR ================= -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm sticky-top" style="z-index: 1000;">

        <div class="container">

            <!-- Hamburger Menu (Always visible) -->
            <button class="btn btn-light border-0 me-2 shadow-sm rounded-3" type="button" data-bs-toggle="offcanvas" data-bs-target="#mainMenuOffcanvas">
                <i class="bi bi-list fs-4"></i>
            </button>

            <a class="navbar-brand d-flex align-items-center text-dark" asp-controller="Home" asp-action="Index">
                <i class="bi bi-journal-text fs-3 me-2 text-primary"></i>
                <strong>UniDoc</strong>
            </a>

            @* ===== Phần tìm kiếm & Icons ===== *@
            <div class="d-flex flex-grow-1 justify-content-end align-items-center" id="navbarContent">

                @* Thanh tìm kiếm trên Navbar *@
                <form asp-controller="Course" asp-action="Index" method="get" class="d-flex mx-auto mb-0 px-lg-3 flex-grow-1" style="max-width: 500px;">
                    <div class="input-group">
                        <input type="text" name="query" class="form-control bg-light shadow-none border-0 rounded-start-pill px-4" placeholder="Search for course..." />
                        <button class="btn btn-light bg-light border-0 rounded-end-pill pe-3 text-muted" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>

                @* ===== Khối các Login/ Register (bên phải) ===== *@
                <div class="d-flex align-items-center gap-2">
                    @if (User.Identity.IsAuthenticated)
                    {
                        @* Nút Profile (Icon) *@
                        <a asp-controller="Account" asp-action="Profile" class="btn btn-light border-0 fs-5 rounded-circle p-2 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;" title="Profile">
                            <i class="bi bi-person-circle text-primary"></i>
                        </a>

                        @* Check role là Admin (Icon) *@
                        @if (User.IsInRole("Admin"))
                        {
                            <a asp-controller="Admin" asp-action="Dashboard" class="btn btn-light border-0 fs-5 rounded-circle p-2 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;" title="Admin Dashboard">
                                <i class="bi bi-shield-lock-fill text-warning"></i>
                            </a>
                        }

                        @* Đăng xuất (Icon) *@
                        <a asp-controller="Account" asp-action="Logout" class="btn btn-light border-0 fs-5 rounded-circle p-2 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;" title="Logout">
                            <i class="bi bi-box-arrow-right text-danger"></i>
                        </a>
                    }
                    else
                    {
                        <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary btn-sm fw-bold px-3">Login</a>
                        <a asp-controller="Account" asp-action="Register" class="btn btn-primary btn-sm px-3 d-none d-md-inline">Register</a>
                    }
                </div>

            </div>

        </div>

    </nav>

    <!-- Offcanvas Main Menu -->
    <div class="offcanvas offcanvas-start" tabindex="-1" id="mainMenuOffcanvas" style="width: 280px;">
        <div class="offcanvas-header border-bottom">
            <h5 class="offcanvas-title fw-bold d-flex align-items-center">
                <i class="bi bi-journal-text fs-3 me-2 text-primary"></i> UniDoc
            </h5>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
        </div>
        <div class="offcanvas-body p-0">
            <div class="list-group list-group-flush">
                <a asp-controller="Home" asp-action="Index" class="list-group-item list-group-item-action py-3 border-0 fw-medium @(currentController == "Home" ? "text-primary bg-primary-subtle" : "text-dark")">
                    <i class="bi bi-house-door me-2"></i> Home
                </a>
                <a asp-controller="Course" asp-action="Index" class="list-group-item list-group-item-action py-3 border-0 fw-medium @(currentController == "Course" ? "text-primary bg-primary-subtle" : "text-dark")">
                    <i class="bi bi-book me-2"></i> Course
                </a>
                <a asp-controller="Document" asp-action="Upload" class="list-group-item list-group-item-action py-3 border-0 fw-medium @(currentController == "Document" ? "text-primary bg-primary-subtle" : "text-dark")">
                    <i class="bi bi-cloud-arrow-up me-2"></i> Attribute
                </a>
            </div>
        </div>
    </div>
    
    @* Toast thông báo toàn trang *@
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="position-fixed top-0 end-0 p-3" style="z-index: 9999">
            <div class="toast align-items-center text-white bg-success border-0 show" role="alert">
                <div class="d-flex">
                    <div class="toast-body fw-bold">
                        <i class="bi bi-check-circle me-2"></i>@TempData["SuccessMessage"]
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                </div>
            </div>
        </div>
    }

    <!-- ================= RENDER BODY ================= -->
    <!-- Đây là nơi phần "ruột" của các trang (như Trang chủ, Đăng nhập) sẽ được nhét vào -->
    <main role="main">
        @RenderBody()
    </main>

    <!-- ================= FOOTER ================= -->
    <footer class="bg-white border-top text-dark py-4 mt-auto">
        <div class="container text-center">
            <p class="mb-1"><strong>UniDocs</strong></p>
            <p class="small text-muted mb-0">
                Gia Phú - Chí Kiệt - Điền Viên
            </p>
        </div>
    </footer>

    <!-- Link JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/main.js"></script>

    <!-- Nơi chứa các script riêng của từng trang (sẽ học sau) -->
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

