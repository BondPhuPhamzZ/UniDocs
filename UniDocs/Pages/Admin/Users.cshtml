@page
@model UniDocs.Pages.Admin.UsersModel
@{
    ViewData["Title"] = "Quản lý Tài khoản";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold text-dark mb-0">Quản lý Tài khoản</h2>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
        <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card border-0 shadow-sm rounded-4">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="px-4 py-3">ID</th>
                        <th class="py-3">Họ và tên</th>
                        <th class="py-3">Email</th>
                        <th class="py-3 text-center">SL Tài liệu</th>
                        <th class="py-3">Trạng thái</th>
                        <th class="px-4 py-3 text-end">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.UserList)
                    {
                        <tr>
                            <td class="px-4 py-3 fw-bold text-muted">#@user.Id</td>
                            <td class="py-3 fw-medium text-dark">@user.LastName @user.FirstName</td>
                            <td class="py-3 text-muted">@user.Email</td>
                            <td class="py-3 text-center">
                                <span class="badge bg-primary rounded-pill px-3 py-2">
                                    @(user.Documents?.Count ?? 0)
                                </span>
                            </td>
                            <td class="py-3">
                                @if (user.IsActive)
                                {
                                    <span class="badge bg-success px-3 py-2 rounded-pill"><i class="bi bi-person-check-fill me-1"></i> Hoạt động</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger px-3 py-2 rounded-pill"><i class="bi bi-person-x-fill me-1"></i> Bị khóa</span>
                                }
                            </td>
                            <td class="px-4 py-3 text-end">
                                <form method="post" asp-page-handler="ToggleLock" asp-route-id="@user.Id" class="d-inline">
                                    @if (user.IsActive)
                                    {
                                        <button type="submit" class="btn btn-outline-danger btn-sm px-3 fw-bold">
                                            <i class="bi bi-lock-fill"></i> Khóa
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="submit" class="btn btn-outline-success btn-sm px-3 fw-bold">
                                            <i class="bi bi-unlock-fill"></i> Mở khóa
                                        </button>
                                    }
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
